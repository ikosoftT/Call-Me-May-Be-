[project]
name = "call-me-maybe"
version = "3.0.0"
description = "Simple implementation for Call Me Maybe"
requires-python = ">=3.10"
dependencies = [
    "pydantic>=2.0",
]

[tool.uv]
package = false
